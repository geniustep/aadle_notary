# Aadle Notary - نظام إدارة الوثائق العدلية 📜

## نظرة عامة

**Aadle Notary** هو نظام متكامل لإدارة الوثائق العدلية في Odoo 18 Community. يوفر حلاً شاملاً لإدارة الوثائق، الزبائن، والفواتير مع workflows متقدمة وصلاحيات محكمة.

---

## الميزات الرئيسية ✨

### 1. إدارة أنواع الوثائق
- تعريف أنواع الوثائق المختلفة (عقود زواج، رسوم إرث، توكيلات، إلخ)
- تحديد أسعار افتراضية لكل نوع
- Sequences تلقائية مخصصة لكل نوع
- تفعيل/تعطيل الأنواع

### 2. إدارة الوثائق
- إنشاء وثائق عدلية بأنواعها المختلفة
- حقول JSON مرنة لبيانات الوثائق
- Workflows متكاملة: مسودة → مؤكد → مُتمّ
- إنشاء فواتير تلقائي
- تتبع كامل (Chatter & Activities)
- ربط بالزبائن والعدول

### 3. إدارة الزبائن
- معلومات موسعة (رقم البطاقة الوطنية)
- عرض جميع وثائق الزبون
- إحصائيات (عدد الوثائق، المبالغ المفوترة)
- Smart Buttons للوصول السريع

### 4. إدارة الفواتير
- إنشاء فواتير تلقائي عند إنشاء الوثيقة
- ربط الفواتير بالوثائق
- تتبع حالة الدفع
- منع إتمام الوثيقة قبل دفع الفاتورة

### 5. الصلاحيات والأمان
- 3 مستويات صلاحيات: مستخدم، مدير، إداري
- Record Rules متقدمة
- العدل يرى وثائقه فقط
- المدير يرى وثائق مكتبه
- الإداري يرى كل شيء

### 6. تكامل جاهز مع APIs
- تجهيز لتوليد PDF (aadle_docgen)
- تجهيز لحساب الإرث الشرعي (aadle_api)
- واجهة JSON-RPC كاملة

---

## التثبيت 🔧

### المتطلبات
- Odoo 18 Community Edition
- Python 3.10+
- PostgreSQL 12+

### خطوات التثبيت

1. **نسخ الـ Module إلى مجلد addons:**
```bash
cd /path/to/odoo/addons
git clone <repository_url> aadle_notary
```

2. **تحديث قائمة التطبيقات:**
```bash
# إعادة تشغيل Odoo مع تحديث قائمة Apps
./odoo-bin -c odoo.conf -d your_database -u all
```

أو من واجهة Odoo:
- الإعدادات → Apps → تحديث قائمة التطبيقات

3. **تثبيت الـ Module:**
- ابحث عن "Aadle Notary" أو "النظام العدلي"
- اضغط "تثبيت"

4. **منح الصلاحيات:**
- الإعدادات → المستخدمين والشركات → المستخدمين
- اختر المستخدم → علامة التبويب "الصلاحيات"
- في قسم "النظام العدلي"، اختر المستوى المناسب

---

## الهيكل 📁

```
aadle_notary/
├── __init__.py                          # ملف التهيئة الرئيسي
├── __manifest__.py                      # معلومات الـ Module
├── models/                              # النماذج
│   ├── __init__.py
│   ├── notary_document_type.py          # أنواع الوثائق
│   ├── notary_document.py               # الوثائق الرئيسية
│   ├── res_partner.py                   # تمديد الزبائن
│   └── account_move.py                  # تمديد الفواتير
├── views/                               # الواجهات
│   ├── notary_document_type_views.xml   # واجهات أنواع الوثائق
│   ├── notary_document_views.xml        # واجهات الوثائق
│   └── notary_menus.xml                 # القوائم
├── security/                            # الصلاحيات والأمان
│   ├── ir.model.access.csv              # صلاحيات الوصول
│   └── notary_security.xml              # المجموعات والقواعد
├── data/                                # البيانات الافتراضية
│   ├── ir_sequence_data.xml             # التسلسلات
│   └── notary_document_type_data.xml    # أنواع الوثائق الافتراضية
└── README.md                            # هذا الملف
```

---

## النماذج (Models) 📊

### 1. `notary.document.type` - أنواع الوثائق

**الغرض:** تعريف أنواع الوثائق المتاحة

**الحقول الرئيسية:**
| الحقل | النوع | الوصف |
|------|------|-------|
| `name` | Char | اسم النوع (عربي) |
| `name_fr` | Char | اسم النوع (فرنسي) |
| `code` | Char | كود فريد |
| `sequence_prefix` | Char | بادئة الترقيم |
| `default_price` | Float | السعر الافتراضي |
| `is_active` | Boolean | فعّال/غير فعّال |

**أنواع الوثائق الافتراضية:**
- عقد زواج (500 درهم) - `MC-YYYY-XXXXXX`
- رسم إرث (800 درهم) - `IH-YYYY-XXXXXX`
- عقد طلاق (400 درهم) - `DC-YYYY-XXXXXX`
- توكيل (300 درهم) - `PA-YYYY-XXXXXX`
- تصديق (200 درهم) - `CR-YYYY-XXXXXX`
- عقد بيع عقار (1000 درهم) - `PS-YYYY-XXXXXX`
- وصية (600 درهم) - `WL-YYYY-XXXXXX`

---

### 2. `notary.document` - الوثائق

**الغرض:** إدارة الوثائق العدلية

**الحقول الرئيسية:**
| الحقل | النوع | الوصف |
|------|------|-------|
| `name` | Char | رقم الوثيقة (تلقائي) |
| `document_type_id` | Many2one | نوع الوثيقة |
| `partner_id` | Many2one | الزبون |
| `notary_id` | Many2one | العدل |
| `date_created` | Date | تاريخ الإنشاء |
| `date_finalized` | Date | تاريخ الإتمام |
| `data` | Json | بيانات الوثيقة المرنة |
| `state` | Selection | الحالة |
| `price` | Float | السعر |
| `invoice_id` | Many2one | الفاتورة المرتبطة |
| `pdf_file` | Binary | ملف PDF |
| `file_hash` | Char | SHA256 hash |

**الحالات (States):**
- `draft` - مسودة
- `confirmed` - مؤكد
- `finalized` - مُتمّ
- `cancelled` - ملغي

**Methods المهمة:**
```python
action_confirm()             # تأكيد الوثيقة
action_finalize()            # إتمام الوثيقة
action_cancel()              # إلغاء الوثيقة
action_create_invoice()      # إنشاء فاتورة
action_generate_pdf()        # توليد PDF (TODO)
action_calculate_inheritance() # حساب الإرث (TODO)
```

---

### 3. `res.partner` - الزبائن (تمديد)

**الحقول المضافة:**
| الحقل | النوع | الوصف |
|------|------|-------|
| `is_client` | Boolean | زبون عدلي |
| `national_id` | Char | رقم البطاقة الوطنية |
| `document_ids` | One2many | الوثائق |
| `document_count` | Integer | عدد الوثائق |
| `total_invoiced` | Monetary | إجمالي المبالغ |

---

### 4. `account.move` - الفواتير (تمديد)

**الحقول المضافة:**
| الحقل | النوع | الوصف |
|------|------|-------|
| `notary_document_id` | Many2one | الوثيقة المرتبطة |
| `is_notary_invoice` | Boolean | فاتورة عدلية |

---

## Workflows 🔄

### 1. إنشاء وثيقة جديدة

```
1. المستخدم ينشئ وثيقة جديدة (state = draft)
   ↓
2. يختار نوع الوثيقة (السعر يُملأ تلقائياً)
   ↓
3. يدخل الزبون والبيانات
   ↓
4. عند الحفظ:
   - يُنشأ رقم تلقائي (sequence)
   - تُنشأ فاتورة تلقائياً
   ↓
5. يمكن التعديل والإضافة
```

### 2. تأكيد الوثيقة

```
الشرط: البيانات كاملة

draft → confirmed

الإجراءات:
- تحديث الحالة
- تسجيل في Chatter
- إشعار العدل
```

### 3. إتمام الوثيقة

```
الشرط:
- state = confirmed
- الفاتورة مدفوعة

confirmed → finalized

الإجراءات:
- تحديث الحالة
- حفظ تاريخ الإتمام
- منع التعديل نهائياً
- تسجيل في Chatter
```

### 4. توليد PDF (TODO)

```
يمكن في أي وقت (draft أو confirmed)

الإجراءات:
- استدعاء aadle_docgen API
- استقبال الملف
- حفظ URL و Hash
- إرفاق الملف
```

---

## الصلاحيات 🔐

### المجموعات (Groups)

#### 1. Notary User (مستخدم عادي)
**XML ID:** `aadle_notary.group_notary_user`

**الصلاحيات:**
- ✅ عرض وثائقه الخاصة
- ✅ إنشاء وثائق جديدة
- ✅ تعديل وثائقه (في حالة draft)
- ✅ تأكيد وثائقه
- ❌ إتمام الوثائق
- ❌ عرض وثائق الآخرين

**Record Rule:**
```python
[('notary_id', '=', user.id)]
```

#### 2. Notary Manager (مدير)
**XML ID:** `aadle_notary.group_notary_manager`

**الصلاحيات:**
- ✅ كل صلاحيات User
- ✅ إتمام الوثائق
- ✅ عرض وثائق المكتب بالكامل
- ✅ تعديل الأسعار
- ✅ إلغاء الوثائق

**Record Rule:**
```python
[('company_id', '=', user.company_id.id)]
```

#### 3. Notary Admin (إداري)
**XML ID:** `aadle_notary.group_notary_admin`

**الصلاحيات:**
- ✅ كل الصلاحيات
- ✅ إدارة أنواع الوثائق
- ✅ عرض كل الوثائق
- ✅ تعديل أي شيء

**Record Rule:**
```python
[(1, '=', 1)]  # لا قيود
```

---

## الاستخدام 💡

### إنشاء وثيقة

1. افتح: **النظام العدلي → الوثائق → كل الوثائق**
2. اضغط **إنشاء**
3. املأ البيانات:
   - **نوع الوثيقة:** اختر من القائمة
   - **الزبون:** اختر أو أنشئ زبوناً جديداً
   - **البيانات:** أدخل البيانات بصيغة JSON
4. اضغط **حفظ** (ستُنشأ فاتورة تلقائياً)
5. اضغط **تأكيد** عندما تكون البيانات جاهزة

### دفع الفاتورة

1. من نموذج الوثيقة، اضغط على زر **الفاتورة**
2. سجّل الدفع
3. ارجع للوثيقة

### إتمام الوثيقة

1. تأكد أن الفاتورة مدفوعة
2. اضغط **إتمام**
3. الوثيقة الآن لا يمكن تعديلها

### إدارة أنواع الوثائق

1. افتح: **النظام العدلي → الإعدادات → أنواع الوثائق**
2. للإنشاء: اضغط **إنشاء**
3. للتعديل: افتح النوع المطلوب
4. يمكن تفعيل/تعطيل الأنواع

---

## API Integration 🔌

### JSON-RPC Endpoints

#### 1. إنشاء وثيقة

```python
# Request
POST /web/dataset/call_kw
{
    "model": "notary.document",
    "method": "create",
    "args": [{
        "document_type_id": 1,
        "partner_id": 10,
        "data": {
            "groom": {"name": "أحمد محمد", "id": "AB123456"},
            "bride": {"name": "فاطمة علي", "id": "CD789012"},
            "dowry": {"amount": 50000, "currency": "MAD"}
        },
        "price": 500
    }],
    "kwargs": {}
}

# Response
{
    "result": 15  # ID الوثيقة الجديدة
}
```

#### 2. قراءة وثيقة

```python
# Request
POST /web/dataset/call_kw
{
    "model": "notary.document",
    "method": "read",
    "args": [[15], ["name", "state", "data", "price", "invoice_state"]],
    "kwargs": {}
}

# Response
{
    "result": [{
        "id": 15,
        "name": "MC-2025-000001",
        "state": "draft",
        "data": {...},
        "price": 500.0,
        "invoice_state": "not_paid"
    }]
}
```

#### 3. تحديث وثيقة

```python
# Request
POST /web/dataset/call_kw
{
    "model": "notary.document",
    "method": "write",
    "args": [[15], {"price": 550}],
    "kwargs": {}
}

# Response
{
    "result": true
}
```

#### 4. البحث والفلترة

```python
# Request
POST /web/dataset/call_kw
{
    "model": "notary.document",
    "method": "search_read",
    "args": [],
    "kwargs": {
        "domain": [["state", "=", "draft"]],
        "fields": ["name", "partner_id", "price"],
        "limit": 10
    }
}

# Response
{
    "result": [
        {"id": 15, "name": "MC-2025-000001", ...},
        {"id": 16, "name": "IH-2025-000001", ...}
    ]
}
```

#### 5. تنفيذ Actions

```python
# تأكيد وثيقة
POST /web/dataset/call_kw
{
    "model": "notary.document",
    "method": "action_confirm",
    "args": [[15]],
    "kwargs": {}
}

# إتمام وثيقة
POST /web/dataset/call_kw
{
    "model": "notary.document",
    "method": "action_finalize",
    "args": [[15]],
    "kwargs": {}
}
```

### Authentication

```python
# Login
POST /web/session/authenticate
{
    "jsonrpc": "2.0",
    "params": {
        "db": "your_database",
        "login": "user@example.com",
        "password": "password"
    }
}

# Response contains session_id in cookies
```

---

## التكامل المستقبلي 🚀

### 1. aadle_docgen (توليد الوثائق)

**الغرض:** توليد PDF للوثائق من قوالب

**Status:** TODO (الكود جاهز للتكامل)

```python
# في notary_document.py → action_generate_pdf()
response = requests.post(
    'http://aadle-docgen-api/generate',
    json={
        'template_id': self.document_type_id.template_id,
        'data': self.data,
    }
)
self.pdf_file = response.content
self.file_hash = hashlib.sha256(response.content).hexdigest()
```

### 2. aadle_api (حساب الإرث)

**الغرض:** حساب الإرث الشرعي

**Status:** TODO (الكود جاهز للتكامل)

```python
# في notary_document.py → action_calculate_inheritance()
response = requests.post(
    'http://aadle-api/calculate-inheritance',
    json=self.data
)
self.data['inheritance_calculation'] = response.json()
```

---

## الاختبار 🧪

### Manual Testing

1. **إنشاء أنواع وثائق:**
   - تحقق من وجود الأنواع الافتراضية
   - جرّب إنشاء نوع جديد

2. **إنشاء وثيقة:**
   - أنشئ زبوناً جديداً
   - أنشئ وثيقة مرتبطة به
   - تحقق من إنشاء الفاتورة تلقائياً

3. **Workflow:**
   - أكّد الوثيقة
   - ادفع الفاتورة
   - أتمّ الوثيقة
   - حاول التعديل (يجب أن يُمنع)

4. **الصلاحيات:**
   - سجّل الدخول كمستخدم عادي (تحقق من رؤية وثائقه فقط)
   - سجّل الدخول كمدير (تحقق من رؤية وثائق المكتب)

### API Testing (Postman)

انظر قسم API Integration أعلاه للأمثلة.

---

## المشاكل الشائعة 🔧

### 1. لا يمكن إتمام الوثيقة

**السبب:** الفاتورة غير مدفوعة

**الحل:** ادفع الفاتورة أولاً

### 2. لا أرى وثائق الآخرين

**السبب:** أنت مستخدم عادي

**الحل:** اطلب من الإداري ترقيتك لمدير

### 3. لا يمكن تعديل السعر

**السبب:** الوثيقة مُتمة

**الحل:** لا يمكن تعديل وثيقة مُتمة

### 4. خطأ عند إنشاء فاتورة

**السبب:** لا يوجد journal مبيعات أو حساب دخل

**الحل:** أنشئ journal و account في المحاسبة

---

## المساهمة 🤝

للمساهمة في تطوير الـ Module:

1. Fork the repository
2. Create a feature branch
3. Make your changes
4. Test thoroughly
5. Submit a pull request

---

## الترخيص 📄

LGPL-3

---

## الدعم 💬

للأسئلة والدعم:
- **الموقع:** https://aadle.com
- **البريد:** support@aadle.com

---

## التحديثات المستقبلية 📅

### v1.1.0 (قريباً)
- [ ] تكامل aadle_docgen
- [ ] تكامل aadle_api
- [ ] تقارير متقدمة
- [ ] Dashboard إحصائي

### v1.2.0
- [ ] تطبيق موبايل
- [ ] إشعارات متقدمة
- [ ] QR Code للتحقق
- [ ] توقيع إلكتروني

---

**Made with ❤️ by Aadle Team**
