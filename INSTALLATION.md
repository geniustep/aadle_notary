# دليل التثبيت - Aadle Notary

## خطوات التثبيت الكاملة في Odoo 18

---

## 📋 المتطلبات الأساسية

### 1. متطلبات النظام

```bash
# نظام التشغيل
Ubuntu 20.04 LTS أو أحدث / Debian 11 أو أحدث

# Python
Python 3.10 أو أحدث

# PostgreSQL
PostgreSQL 12 أو أحدث

# الذاكرة
4GB RAM على الأقل (يُنصح بـ 8GB)

# المساحة
2GB مساحة خالية على الأقل
```

### 2. Odoo 18 Community

يجب أن يكون لديك Odoo 18 Community مثبت ويعمل.

**للتحقق من النسخة:**
```bash
./odoo-bin --version
# يجب أن تظهر: Odoo Server 18.0
```

---

## 🔧 التثبيت - الطريقة الأولى (Development)

### الخطوة 1: نسخ الـ Module

```bash
# الانتقال لمجلد addons
cd /path/to/odoo/addons

# نسخ الـ Module من Git
git clone https://github.com/aadle/aadle_notary.git

# أو نسخ يدوياً
mkdir aadle_notary
# ثم نسخ جميع الملفات إلى aadle_notary/
```

### الخطوة 2: التحقق من صلاحيات الملفات

```bash
# التأكد من أن Odoo يمكنه قراءة الملفات
sudo chown -R odoo:odoo aadle_notary
sudo chmod -R 755 aadle_notary
```

### الخطوة 3: إعادة تشغيل Odoo

```bash
# إيقاف Odoo
sudo systemctl stop odoo

# بدء Odoo مع تحديث قائمة Apps
./odoo-bin -c /etc/odoo/odoo.conf -d your_database -u all

# أو
sudo systemctl start odoo
```

### الخطوة 4: تفعيل Developer Mode

1. افتح Odoo في المتصفح: `http://localhost:8069`
2. سجل الدخول كـ Admin
3. اذهب إلى: **الإعدادات → Activate the developer mode**

### الخطوة 5: تحديث قائمة التطبيقات

1. اذهب إلى: **Apps**
2. اضغط على القائمة (≡) → **Update Apps List**
3. اضغط **Update**

### الخطوة 6: البحث والتثبيت

1. في شريط البحث في Apps، اكتب: `aadle notary` أو `النظام العدلي`
2. يجب أن تظهر بطاقة Module
3. اضغط **Install**

### الخطوة 7: التحقق من التثبيت

بعد التثبيت:
- يجب أن يظهر قائمة جديدة في الـ navbar: **النظام العدلي**
- اضغط عليها للتأكد من عمل Module

---

## 🎯 التثبيت - الطريقة الثانية (Production)

### الخطوة 1: إضافة Module للـ addons path

```bash
# تعديل ملف odoo.conf
sudo nano /etc/odoo/odoo.conf

# إضافة أو تعديل السطر:
addons_path = /usr/lib/python3/dist-packages/odoo/addons,/path/to/custom/addons,/path/to/aadle_notary
```

### الخطوة 2: تثبيت عبر Command Line

```bash
# إيقاف Odoo
sudo systemctl stop odoo

# تثبيت Module
/usr/bin/odoo -c /etc/odoo/odoo.conf -d production_db -i aadle_notary --stop-after-init

# بدء Odoo
sudo systemctl start odoo
```

---

## ⚙️ الإعدادات بعد التثبيت

### 1. إنشاء المجموعات والصلاحيات

#### تفعيل صلاحيات المستخدمين:

1. اذهب إلى: **الإعدادات → المستخدمون والشركات → المستخدمين**
2. اختر المستخدم
3. انتقل إلى تبويب **Access Rights**
4. في قسم **النظام العدلي**، اختر المستوى:
   - **مستخدم عدلي** - للعدول العاديين
   - **مدير عدلي** - للمدراء
   - **إداري عدلي** - للإداريين

### 2. إعداد المحاسبة (إذا لم يكن معداً)

#### إنشاء Journal المبيعات:

1. اذهب إلى: **المحاسبة → الإعدادات → Journals**
2. إذا لم يكن موجوداً، أنشئ Journal جديد:
   - **الاسم:** مبيعات
   - **النوع:** Sales
   - **الشركة:** اختر شركتك

#### إنشاء حساب الدخل:

1. اذهب إلى: **المحاسبة → الإعدادات → Chart of Accounts**
2. أنشئ حساب جديد:
   - **الكود:** 701000 (مثلاً)
   - **الاسم:** دخل الوثائق العدلية
   - **النوع:** Income
   - **الشركة:** اختر شركتك

### 3. التحقق من البيانات الافتراضية

#### التحقق من أنواع الوثائق:

1. اذهب إلى: **النظام العدلي → الإعدادات → أنواع الوثائق**
2. يجب أن تجد 7 أنواع افتراضية:
   - عقد زواج (500 درهم)
   - رسم إرث (800 درهم)
   - عقد طلاق (400 درهم)
   - توكيل (300 درهم)
   - تصديق (200 درهم)
   - عقد بيع عقار (1000 درهم)
   - وصية (600 درهم)

#### التحقق من Sequences:

1. اذهب إلى: **الإعدادات → Technical → Sequences**
2. ابحث عن: `notary.document`
3. يجب أن تجد sequences لكل نوع وثيقة

---

## 🧪 اختبار التثبيت

### Test 1: إنشاء زبون

1. اذهب إلى: **Contacts → Create**
2. املأ البيانات:
   - **الاسم:** أحمد محمد
   - **زبون عدلي:** ✓
   - **رقم البطاقة:** AB123456
3. احفظ

### Test 2: إنشاء وثيقة

1. اذهب إلى: **النظام العدلي → الوثائق → كل الوثائق → Create**
2. املأ البيانات:
   - **نوع الوثيقة:** عقد زواج
   - **الزبون:** اختر الزبون الذي أنشأته
   - **البيانات:** `{"test": "data"}`
3. احفظ
4. **تحقق:** يجب أن يُنشأ رقم تلقائي مثل `MC-2025-000001`
5. **تحقق:** يجب أن تُنشأ فاتورة تلقائياً

### Test 3: Workflow

1. من نفس الوثيقة، اضغط **تأكيد**
2. **تحقق:** الحالة تغيرت إلى "مؤكد"
3. اذهب للفاتورة (Smart Button)
4. سجّل دفع الفاتورة
5. ارجع للوثيقة
6. اضغط **إتمام**
7. **تحقق:** الحالة أصبحت "مُتمّ" ولا يمكن التعديل

### Test 4: الصلاحيات

1. أنشئ مستخدم جديد
2. امنحه صلاحية "مستخدم عدلي"
3. سجل الدخول كهذا المستخدم
4. **تحقق:** يرى وثائقه فقط
5. **تحقق:** لا يمكنه إتمام الوثائق

---

## 🔧 حل المشاكل (Troubleshooting)

### مشكلة 1: Module لا يظهر في Apps

**الأعراض:** بعد Update Apps List، لا يظهر Module

**الحلول:**
```bash
# 1. تحقق من المسار
ls -la /path/to/odoo/addons/aadle_notary

# 2. تحقق من __manifest__.py
cat /path/to/odoo/addons/aadle_notary/__manifest__.py

# 3. تحقق من logs
tail -f /var/log/odoo/odoo.log

# 4. أعد تشغيل Odoo مع --update=all
./odoo-bin -c odoo.conf -d your_db --update=all
```

### مشكلة 2: خطأ عند التثبيت

**الأعراض:**
```
Error: Module aadle_notary: Unable to install
```

**الحلول:**
```bash
# 1. تحقق من Dependencies
# في __manifest__.py تأكد من وجود:
'depends': ['base', 'mail', 'account', 'contacts']

# 2. تأكد من تثبيت mail module
# Apps → ابحث عن Discuss → Install

# 3. تحقق من logs
grep -i error /var/log/odoo/odoo.log
```

### مشكلة 3: لا يمكن إنشاء فاتورة

**الأعراض:**
```
Error: لا يوجد journal مبيعات!
```

**الحلول:**
1. اذهب إلى: **المحاسبة → الإعدادات → Journals**
2. أنشئ Journal جديد (نوع: Sales)
3. اذهب إلى: **المحاسبة → الإعدادات → Chart of Accounts**
4. أنشئ حساب دخل (Income)

### مشكلة 4: خطأ في Access Rights

**الأعراض:**
```
Access Denied
You are not allowed to access this document
```

**الحلول:**
1. تحقق من الصلاحيات:
   - الإعدادات → المستخدمون → اختر المستخدم
   - تأكد من وجود صلاحية النظام العدلي
2. تحقق من Record Rules:
   - الإعدادات → Technical → Record Rules
   - ابحث عن "notary.document"
3. إذا لزم الأمر، أعد تثبيت Module

### مشكلة 5: Sequence لا يعمل

**الأعراض:** رقم الوثيقة يظهر "/" أو خطأ

**الحلول:**
```bash
# 1. تحقق من Sequences
# الإعدادات → Technical → Sequences
# ابحث عن: notary.document

# 2. إذا لم تكن موجودة، أعد تثبيت data files
./odoo-bin -c odoo.conf -d your_db -i aadle_notary --stop-after-init

# 3. أو أنشئ sequences يدوياً من الواجهة
```

---

## 🔄 التحديث (Upgrade)

### تحديث Module لنسخة أحدث

```bash
# 1. نسخة احتياطية من قاعدة البيانات
pg_dump your_database > backup_$(date +%Y%m%d).sql

# 2. سحب آخر تحديثات
cd /path/to/odoo/addons/aadle_notary
git pull

# 3. ترقية Module
./odoo-bin -c odoo.conf -d your_db -u aadle_notary

# 4. إعادة تشغيل Odoo
sudo systemctl restart odoo
```

---

## 🗑️ إلغاء التثبيت

### حذف Module

**⚠️ تحذير:** سيتم حذف جميع البيانات!

```bash
# 1. من واجهة Odoo
# Apps → ابحث عن Module → Uninstall

# 2. أو من Command Line
./odoo-bin -c odoo.conf -d your_db --uninstall=aadle_notary

# 3. حذف ملفات Module
rm -rf /path/to/odoo/addons/aadle_notary
```

---

## 📦 التثبيت في بيئات متعددة

### Development Environment

```bash
# استخدام database منفصلة
./odoo-bin -c odoo.conf -d aadle_dev -i aadle_notary

# تفعيل developer mode
# واجهة Odoo → الإعدادات → Activate developer mode
```

### Staging Environment

```bash
# نسخ database production
pg_dump production_db | psql staging_db

# تثبيت في staging
./odoo-bin -c odoo.conf -d staging_db -i aadle_notary
```

### Production Environment

```bash
# 1. نسخة احتياطية أولاً!
pg_dump production_db > backup.sql

# 2. اختبار في staging أولاً

# 3. تثبيت في production
./odoo-bin -c /etc/odoo/odoo.conf -d production_db -i aadle_notary --stop-after-init

# 4. تحقق من logs
tail -f /var/log/odoo/odoo.log
```

---

## 🐳 التثبيت باستخدام Docker

### docker-compose.yml

```yaml
version: '3.8'

services:
  odoo:
    image: odoo:18.0
    depends_on:
      - db
    ports:
      - "8069:8069"
    volumes:
      - ./aadle_notary:/mnt/extra-addons/aadle_notary
      - odoo-data:/var/lib/odoo
    environment:
      - HOST=db
      - USER=odoo
      - PASSWORD=odoo

  db:
    image: postgres:15
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=odoo
      - POSTGRES_PASSWORD=odoo
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  odoo-data:
  postgres-data:
```

### التشغيل

```bash
# 1. وضع aadle_notary في نفس مجلد docker-compose.yml

# 2. بدء الـ containers
docker-compose up -d

# 3. تثبيت Module
# افتح http://localhost:8069
# أنشئ database جديدة
# Apps → ابحث عن aadle_notary → Install
```

---

## 📊 متطلبات الأداء

### للاستخدام الخفيف (< 100 وثيقة/يوم)
```
CPU: 2 cores
RAM: 4GB
Storage: 20GB SSD
```

### للاستخدام المتوسط (100-500 وثيقة/يوم)
```
CPU: 4 cores
RAM: 8GB
Storage: 50GB SSD
```

### للاستخدام الكثيف (> 500 وثيقة/يوم)
```
CPU: 8+ cores
RAM: 16GB+
Storage: 100GB+ SSD
Database: PostgreSQL cluster
```

---

## ✅ Checklist بعد التثبيت

- [ ] Module مثبت وظاهر في Apps
- [ ] قائمة "النظام العدلي" ظاهرة
- [ ] 7 أنواع وثائق موجودة
- [ ] Sequences تعمل
- [ ] يمكن إنشاء زبون جديد
- [ ] يمكن إنشاء وثيقة جديدة
- [ ] الفاتورة تُنشأ تلقائياً
- [ ] Workflow يعمل (تأكيد → إتمام)
- [ ] الصلاحيات تعمل بشكل صحيح
- [ ] لا توجد أخطاء في logs

---

## 📞 الدعم

إذا واجهت مشاكل في التثبيت:

1. تحقق من الـ logs: `/var/log/odoo/odoo.log`
2. راجع قسم Troubleshooting أعلاه
3. ابحث في GitHub Issues
4. اتصل بالدعم: support@aadle.com

---

## 🎓 الخطوات التالية

بعد التثبيت الناجح:

1. **راجع الوثائق:**
   - [README.md](README.md) - نظرة عامة
   - [TECHNICAL_SUMMARY.md](TECHNICAL_SUMMARY.md) - تفاصيل تقنية
   - [POSTMAN_COLLECTION.md](POSTMAN_COLLECTION.md) - API testing

2. **أنشئ بيانات تجريبية:**
   - زبائن
   - وثائق بأنواع مختلفة
   - اختبر جميع الـ workflows

3. **ادرب المستخدمين:**
   - كيفية إنشاء وثيقة
   - كيفية تتبع الفواتير
   - كيفية البحث والفلترة

4. **أعد الإعدادات:**
   - أسعار الوثائق (حسب الحاجة)
   - صلاحيات المستخدمين
   - تخصيص الواجهة

---

**تم التثبيت بنجاح! 🎉**

**Made with ❤️ by Aadle Team**
